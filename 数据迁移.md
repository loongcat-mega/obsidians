![image.png](https://yaaame-1317851743.cos.ap-beijing.myqcloud.com/20250804213747.png)
![image.png](https://yaaame-1317851743.cos.ap-beijing.myqcloud.com/20250804214428.png)


## 双写
在数据迁移过程中，​**双写（Dual Write）​**​ 是一种常见的过渡策略，指的是 ​**同时向新旧两个系统（如旧数据库/旧 Kafka 集群和新数据库/新 Kafka 集群）写入相同的数据**，以确保在迁移期间新旧系统的数据一致性，最终实现平滑切换。
### ​**1. 双写的典型场景**​

#### ​**​(1) 数据库迁移**​

- ​**旧系统**​：MySQL/Oracle 等传统数据库
- ​**新系统**​：分布式数据库（如 TiDB）、云数据库（如 AWS RDS）或分库分表方案
- ​**双写操作**​：应用层同时向旧库和新库写入同一条数据（如订单、用户信息）。

#### ​**​(2) Kafka 集群迁移**​

- ​**旧集群（A）​**​：源 Kafka 集群（如自建集群）
- ​**新集群（B）​**​：目标 Kafka 集群（如云服务 Kafka 或升级后的集群）
- ​**双写操作**​：生产者同时向旧集群 Topic 和新集群 Topic 发送相同的消息。

### **2. Kafka 双写的具体实现**​
##### **方式 1：应用层双写（推荐可控方案）​**​

- ​**逻辑**​：修改业务代码，让生产者显式向 ​**旧集群和新集群的 Topic**​ 同时发送消息。
- ​**优点**​：完全可控，可灵活处理异常（如重试、降级）。
- ​**缺点**​：需修改代码，维护成本高。

##### ​**方式 2：中间件层双写（透明化方案）​**​

- ​**逻辑**​：通过 ​**消息网关、代理层或 Kafka Connect**​ 拦截生产者的消息，并同时转发到新旧集群。
    - 例如：使用 ​**Kafka MirrorMaker**​ 或自定义代理服务，将写入旧集群的消息镜像到新集群。
- ​**优点**​：无需修改业务代码，对应用透明。
- ​**缺点**​：依赖中间件稳定性，故障排查复杂。

##### **方式 3：变更数据捕获（CDC）补充双写**​

- ​**逻辑**​：如果无法修改生产者代码，可通过 ​**CDC 工具（如 Debezium）​**​ 监听旧集群的 Topic，将消息实时同步到新集群。
- ​**适用场景**​：只读迁移或无法双写的遗留系统。

### **3. 双写的关键问题与解决方案**​

#### ​**问题 1：如何保证双写的一致性？​**​

- ​**挑战**​：若写入旧集群成功但新集群失败（或反之），会导致数据不一致。
- ​**解决方案**​：
    - ​**事务支持**​：使用 Kafka 事务（`enable.idempotence=true` + `transactional.id`）确保原子性。
    - ​**重试机制**​：对失败的消息进行重试（如通过 Dead Letter Queue 缓存异常数据）。
    - ​**校验工具**​：迁移期间对比新旧集群的 offset 或消息内容（如抽样检查）。

#### ​**问题 2：如何逐步切换消费者？​**​

- ​**步骤**​：
    1. ​**双写阶段**​：生产者和消费者均同时读写新旧集群（确保新集群数据完整）。
    2. ​**验证阶段**​：通过监控工具（如 Kafka Lag、数据抽样）确认新集群无延迟或错误。
    3. ​**切换消费者**​：将消费者组逐步迁移到新集群（先非核心业务，后核心业务）。
    4. ​**停用旧集群**​：确认所有消费者稳定运行后，停止向旧集群写入并下线旧集群。

#### ​**问题 3：双写期间的性能影响？​**​

- ​**优化建议**​：
    - 异步发送消息（减少阻塞）。
    - 批量发送（提升吞吐量）。
    - 监控网络带宽和集群负载。

### ​**4. 双写的典型流程（Kafka 迁移示例）​**​

1. ​**准备阶段**​：
    
    - 部署新 Kafka 集群（B），确保 Topic 配置（分区数、保留策略）与旧集群（A）一致。
    - 修改生产者代码或配置，使其同时向 A 和 B 发送消息。
2. ​**双写运行阶段**​：
    
    - 生产者持续向 A 和 B 写入数据，消费者仍从 A 读取。
    - 监控新旧集群的 Lag、消息延迟和错误率。
3. ​**验证阶段**​：
    
    - 使用工具（如 `kafka-diff` 或自定义脚本）对比 A 和 B 的消息内容。
    - 逐步将非核心消费者切换到 B，验证业务逻辑。
4. ​**切换阶段**​：
    
    - 将所有消费者迁移到 B，停止向 A 写入（或降级为只读）。
    - 确认无问题后，下线旧集群 A。