
![image.png](https://yaaame-1317851743.cos.ap-beijing.myqcloud.com/20240729215229.png)


- 本地事务：ACID
- 分布式事务：CAP，BASE，2PC，XA规范
- 数据隔离级别
- 锁：行锁，表锁，数据库锁
- UNDO REDO LOG
# 本地事务

事务是操作数据库中某个数据项的一个程序执行单元。具有四个属性：
## 事务属性 ACID
### 原子性 Atomic
事务必须是一个原子的操作序列单元。事务中包含的各项操作在一次执行过程中，要么全部执行成功，要么全部不执行。任何一项失败，整个事务回滚，只有全部都执行成功，整个事务才算成功。

### 一致性 Consistency

事务的执行不能破坏数据库数据的完整性和一致性，事务在执行之前和之后，数据库都必须处于**一致性状态**。

### 隔离性 Isolation
在并发环境中，并发的事务是相互隔离的，一个事务的执行不能被其他事务干扰。即每个事务都有自己的数据空间。

### 持久性 Durability
一个事务一旦提交，他对数据库中对应数据的状态变更都应该是永久性的。

## MySQL 的本地事务实现方案

以InnoDB为例，是通过**日志和锁**来保证事务的ACID
- 通过数据库锁的机制，保障事务的隔离性
- 通过Redo Log ，保证事务的原子性
- 通过Undo Log，保证事务的原子性和一致性


### Undo Log

在操作数据之前，首先将数据备份到一个地方（这个存储数据备份的地方成为Undo Log），然后进行数据的修改，如果出现了错误或者执行了RollBack，系统就可以利用Undo Log中的备份数据回到开始的状态


### Redo Log

记录的是新数据的备份。在事务提交前，只要将Redo Log持久化即可，不需要将数据持久化。当系统崩溃时，可以根据Redo Log的内容，将所有数据恢复到之前的状态

## 锁

### 本地锁
乐观锁与悲观锁。悲观锁：认为操作会出现数据冲突，所以在每次操作的时候都要通过获取锁才能进行对数据的操作。


按属性分：

- 共享锁
又称读锁，是读取操作创建的锁，其他用户可以并发读取数据，但任何事物都不能对数据进行修改（获取排他锁），直到已释放**所有共享锁**。
在查询语句后面增加LOCK IN SHARE MODE，mysql会对查询结果中的每行都加共享锁，当没有其他线程对查询结果集中任何一行使用排他锁时，可以成功申请共享锁，否则会被阻塞。

-  排他锁
又称写锁，若某个事务对某行加上了排他锁，则只能这个事务对其进行读写。在此事务结束之前，其他事务不能对其加任何锁，其他进程可以读取，不能进行写操作。


按粒度
- 行锁：开销大，加锁慢，粒度小，并发高


- 表锁：开销大，加锁快，粒度大，并发低。

### 分布式锁

暂时使用redis

## 数据库并发操作中的异常情形

### 一类丢失更新

两个事务读同一数据，一个修改字段1，一个修改字段2，后提交的恢复了先提交修改的字段。

### 二类丢失更新

两个事务读同一数据，都修改同一字段，后提交的覆盖了先提交的修改。

### 脏读

读到了未提交的值，万一该事务回滚，则产生脏读
### 不可重复读

两个查询之间，被另一个事务修改（update）了数据内容，产生的内容不一致

### 幻读

两个查询之间，被另一个事务插入或删除了记录，产生的结果集不一致


## 事务隔离级别

| 隔离级别 | 脏读  | 不可重复读 | 幻读  |
| ---- | --- | ----- | --- |
| 读未提交 | 可能  | 可能    | 可能  |
| 读已提交 | 不可能 | 可能    | 可能  |
| 可重复读 | 不可能 | 不可能   | 可能  |
| 可串行化 | 不可能 | 不可能   | 不可能 |
![image.png](https://yaaame-1317851743.cos.ap-beijing.myqcloud.com/20240729211636.png)

数据一致性和高性能是天生的矛盾。Oracle默认事务隔离级别为RC，MySQL默认事务隔离级别为RR
MySQL中查看事务隔离级别
```mysql
SELECT @@global.transaction_isolation;
```

### 读未提交 Read UnCommitted

允许脏读。
如果一个事务正在处理某一数据，并对其进行了更新， 但同时尚未完成事务，或者说事务没有提交， 与此同时，允许另一个事务也能够访问该数据。未提交意味着这些数据可能会回滚，也就是可能最终不会存到数据库中，也就是不存在的数据。

### 读已提交 Read Committed

禁止了脏读，但是允许不可重复读。
只允许读到已经提交的数据。

**事务 A 多次读取同一数据，但事务 B 在事务A多次读取的过程中，对数据作了更新并提交，导致事务A多次读取同一数据时，结果 不一致。**

### 可重复读 Repeatable Read

禁止了脏读，不可重复读。保证在事务处理过程中，多次读取同一个数据时，其值都和事务开始时刻时是一致的。
但是允许幻读。在可重复读中，该SQL第一次读取到数据后，就将这些数据加锁（悲观锁），其他事务无法修改数据，但是这种方法无法锁住insert的数据，所以当事务A先前读取了数据，或者修改了全部数据，事务B还是可以insert数据提交，这时事务A就会发现莫名其妙多了一条之前没有的数据，这就是幻读，不能通过行锁来避免。

### 可串行化 Serializable

最严格的事务，要求所有事务被串行执行，不能并发执行。

# 分布式事务

分布式事务指事务的参与者，支持事务的服务器，资源服务器以及事务管理器分别位于不同的分布式系统的不同节点之上。

典型的分布式事务场景：
- 跨库事务
- 分库分表
- 微服务化


## 事务属性 CAP

一个分布式事务无法同时满足以下三个属性：
- 一致性：客户端知道的一系列操作都会同时发生
- 可用性：每个操作都必须以可预期的响应结束
- 分区容错性：即使出现单个组件无法可用，操作依旧可以完成


![image.png](https://yaaame-1317851743.cos.ap-beijing.myqcloud.com/20240729212124.png)

一个Web应用至多只能同时支持上面的两个属性，因此必须在一致性与可用性之间作出选择。
![image.png](https://yaaame-1317851743.cos.ap-beijing.myqcloud.com/20240729212931.png)

### 一致性 Consistency
all nodes see the same data at the same time

数据操作的一致性与主从之间的一致性

- 强一致性：任意时刻客户端看到的数据都是一致的
- 弱一致性：部分数据不一致
- 最终一致性：存在中间数据，但最终数据一致

### 可用性 Availablity

系统提供的服务必须一致处于可用的状态，对于用户的每一个操作请求总是能够在有限的时间内返回结果

- 有限时间内
- 返回正常结果

### 分区容错性 Partition Tolerance

分布式系统遇到任何网络分区故障时，仍需要能够保证对外提供满足一致性和可用性的服务。


## CAP与ACID

- ACID一致性是有关数据库规则，数据库总是从一个一致性的状态转换到另外一个一致性的状态；
- CAP的一致性是分布式多服务器之间复制数据令这些服务器拥有同样的数据，由于网速限制，这种复制在不同的服务器上所消耗的时间是不固定的，集群通过组织客户端查看不同节点上还未同步的数据维持逻辑视图，这是一种分布式领域的一致性概念；

## BASE 定理

BASE理论是对CAP理论的延伸，核心是想是即使无法做到强一致性，但应用可以采用合适的方法达到最终一致性
牺牲一致性来获得可用性，并允许数据在一段时间内是不一致的，但最终达到一致状态
### 基本可用 Basically Available 

允许损失部分可用性，但不等于系统不可用
- 响应时间上的损失
- 功能上的损失（服务降级）：当流量高峰期时，屏蔽一些功能

### 软状态 Soft State

允许系统中的数据存在中间状态，并认为该中间状态不为影响系统的整体可用性

### 最终一致性 Eventually Consistent

## 分布式事务分类

### 刚性事务

满足CAP理论中的CP理论，通常无业务改造，强一致性，原声支持回滚/隔离性，低并发，适合短事务

XA协议（2PC，JTA），3PC，但由于同步阻塞，处理效率低，不适合大型分布式场景


#### XA 模型 或者 X/Open DTP模型

XA是一个分布式事务模型，这个模型主要使用了两阶段提交2PC(two-phase-commit)来保证事务的完整性

在XA中，有三个角色
- AP：应用程序，也就是业务层
- TM Transaction Manager：事务管理器，接收AP的的事务请求，对全局事务进行管理，管理事务分支状态，协调RM的处理，通知RM哪些操作属于全局事务以及事务分支
- RM Resource Manager ：资源管理器，一般是数据库，也可以是MQ 文件系统等

AP操作TM，当需要事务时，AP向TM发起事务，TM负责整个事务的提交，回滚等

XA规范主要定义了TM与RM之间的接口。XA接口是双向的系统接口，在TM以及多个RM之间形成通信桥梁。

XA是数据库的分布式事务，强一致性，在整个过程中，数据一直处于锁住状态，即从prepare到commit、rollback的整个过程中，TM一直把持着数据库的锁，如果有其他人要修改数据库的该条数据，就必须等待锁的释放，**存在长事务风险**。

![image.png](https://yaaame-1317851743.cos.ap-beijing.myqcloud.com/20240729215007.png)


#### 两阶段提交 2PC

使得所有分布式节点可以在进行事务处理时能够保持原子性和一致性
两阶段：
- 提交事务请求
- 执行事务提交或者执行中断事务

 ##### 阶段一：提交事务请求

1. 事务询问。协调者向所有参与者发送事务内容，询问是否可以执行提交操作，并开始等待各参与者进行响应；
2. 执行事务。各参与者节点，执行事务操作，并将Undo和Redo操作计入本机事务日志；
3. 各参与者向协调者反馈事务问询的响应。成功执行返回Yes，否则返回No。

 ##### 阶段二：执行事务提交，或者执行中断事务;

这一阶段包含两种情形：

- 执行事务提交，
    
- 执行中断事务
    

协调者在阶段二决定是否最终执行事务提交操作。

- 所有参与者reply Yes，那么执行事务提交。
- 当存在某一参与者向协调者发送No响应，或者等待超时。协调者只要无法收到所有参与者的Yes响应，就会中断事务。

 ##### 执行事务提交

所有参与者reply Yes，那么执行事务提交。

1. 发送提交请求。协调者向所有参与者发送Commit请求；
2. 事务提交。参与者收到Commit请求后，会**正式执行事务提交操作**，并在完成提交操作之后，释放在整个事务执行期间占用的资源；
3. 反馈事务提交结果。参与者在完成事务提交后，写协调者发送Ack消息确认；
4. 完成事务。协调者在收到所有参与者的Ack后，完成事务。

 #### 2PC 特点

适合单体性用，跨多个数据库的事务，但是因为严重依赖数据库层面来解决复杂的事务，效率低，不适合高并发的场景。
![image.png](https://yaaame-1317851743.cos.ap-beijing.myqcloud.com/20240729215802.png)

优点：简单
缺点：
- 同步阻塞导致性能问题。执行过程中，所有参与节点都是事务阻塞型的。
- 单点故障导致高可用问题。协调者是个单点，一旦出现问题，各个participant 将无法释放事务资源，也无法完成事务操作。
- 丢失信息导致数据不一致问题。只有一部分参与者接受到了commit请求。


#### 三阶段提交 3PC

![image.png](https://yaaame-1317851743.cos.ap-beijing.myqcloud.com/20240729220240.png)

### 柔性事务

满足BASE理论，有业务改造，实现补偿接口，实现资源锁定接口，高并发，适合长事务

柔性事务分为
- 补偿型
- 异步确保型
- 最大努力通知型

TCC/FMT，Saga，本地事务消息，消息事务（半消息）



